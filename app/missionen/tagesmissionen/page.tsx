"use client";

import Image from "next/image";
import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import { completeMission, startMission } from "@/lib/missionCompletion";
import { listenUserFavorites, toggleMissionFavorite } from "@/lib/missionFavorites";
import { finishTrackingSession, startTrackingSession } from "@/lib/tracking";
import { createBrowserStepCounter } from "@/lib/stepCounter";
import { createAiMissionForCurrentUser } from "@/lib/aiMissionGenerator";
import { calculateRewardMultipliers } from "@/lib/avatarMultiplier";
import { calculateDynamicRewardScore } from "@/lib/rewardScore";
import { economyConfig, getRewardRate } from "@/config/economy";
import { auth, db } from "@/lib/firebase";
import { onAuthStateChanged } from "firebase/auth";
import { collection, onSnapshot, orderBy, query, where } from "firebase/firestore";

type MissionTab = "Tagesmissionen" | "Wochenmissionen" | "Abenteuer" | "Challenge" | "Wettkämpfe" | "Favoriten" | "History";
type Mission = { id: string; numericId: number; title: string; reward: number; baseReward?: number; category: MissionTab; difficulty: "Leicht" | "Mittel" | "Schwer"; description: string; duration: string; type: "Bewegung" | "Ernährung" | "Workout" | "Community" | "Abenteuer"; targetValue?: number; unit?: "steps" | "minutes" | "questions" | "checkin" | string; aiPersonalized?: boolean; source?: "firestore" | "fallback" | "ai"; };
type MissionProgress = { missionId: string; status: string; progressValue: number; pointsGranted?: number; lockedRewardPoints?: number; avatarMultiplier?: number; userEconomyMultiplier?: number; systemRewardRate?: number; precisionFactor?: number; socialMultiplier?: number; streakMultiplier?: number; sponsorMultiplier?: number; integrityMultiplier?: number; multiplierReasons?: string[]; };
type RewardPreview = { finalReward: number; systemRewardRate: number; avatarMultiplier: number; userEconomyMultiplier: number; precisionFactor: number; socialMultiplier: number; streakMultiplier: number; sponsorMultiplier: number; integrityMultiplier: number; validationRisk: number; reasons: string[]; };

const tabs: MissionTab[] = ["Tagesmissionen", "Wochenmissionen", "Abenteuer", "Challenge", "Wettkämpfe", "Favoriten", "History"];
const tabHref = (tab: MissionTab) => tab === "Tagesmissionen" ? "/missionen/tagesmissionen" : tab === "Wochenmissionen" ? "/missionen/wochenmissionen" : tab === "Abenteuer" ? "/missionen/abenteuer" : tab === "Challenge" ? "/missionen/challenge" : tab === "Wettkämpfe" ? "/missionen/wettkaempfe" : tab === "Favoriten" ? "/missionen/favoriten" : "/missionen/history";
const missionIcon = (type: Mission["type"]) => type === "Bewegung" ? "🏃" : type === "Ernährung" ? "📖" : type === "Workout" ? "💪" : type === "Community" ? "👥" : "🔎";

const fallbackMissions: Mission[] = [
  { id: "fallback-daily-steps", numericId: 1, title: "6.000 Schritte", reward: 5, baseReward: 5, category: "Tagesmissionen", difficulty: "Leicht", description: "Erreiche heute 6.000 Schritte und halte deinen Bewegungsfluss aktiv.", duration: "1 Tag", type: "Bewegung", targetValue: 6000, unit: "steps", aiPersonalized: true, source: "fallback" },
  { id: "fallback-daily-water", numericId: 2, title: "Nahrung und Verdauung", reward: 5, baseReward: 5, category: "Tagesmissionen", difficulty: "Leicht", description: "Beantworte eine kurze Gesundheitsfrage zu Nahrung und Verdauung.", duration: "2 Minuten", type: "Ernährung", targetValue: 1, unit: "questions", aiPersonalized: true, source: "fallback" },
  { id: "fallback-daily-workout", numericId: 3, title: "30 Sek. Stärkung", reward: 5, baseReward: 5, category: "Tagesmissionen", difficulty: "Mittel", description: "Absolviere 30 Sekunden Stärkung für deinen Körper.", duration: "30 Sekunden", type: "Workout", targetValue: 1, unit: "checkin", aiPersonalized: true, source: "fallback" },
];

const normalizeMission = (docId: string, data: any, index: number): Mission => ({ id: docId, numericId: typeof data.numericId === "number" ? data.numericId : index + 1, title: String(data.title ?? "KI Mission"), reward: Number(data.pointsReward ?? data.reward ?? data.baseRewardPoints ?? 5), baseReward: Number(data.baseRewardPoints ?? data.baseReward ?? data.pointsReward ?? data.reward ?? 5), category: String(data.category ?? data.displayCategory ?? "Tagesmissionen") as MissionTab, difficulty: (data.difficulty === "Schwer" || data.difficulty === "Mittel" || data.difficulty === "Leicht") ? data.difficulty : "Leicht", description: String(data.description ?? "Diese Mission wurde dynamisch vorbereitet."), duration: String(data.duration ?? data.timeWindow ?? "1 Tag"), type: (data.type === "Ernährung" || data.type === "Workout" || data.type === "Community" || data.type === "Abenteuer" || data.type === "Bewegung") ? data.type : "Bewegung", targetValue: Number(data.targetValue ?? (data.unit === "steps" ? 6000 : 1)), unit: String(data.unit ?? (data.type === "Bewegung" ? "steps" : "checkin")), aiPersonalized: Boolean(data.aiPersonalized ?? data.generatedByAi ?? true), source: data.generatedByAi ? "ai" : "firestore" });
const formatMultiplier = (value?: number) => `×${Number(value ?? 1).toFixed(2)}`;
const multiplierColor = (value?: number) => Number(value ?? 1) > 1 ? "text-green-300" : Number(value ?? 1) < 1 ? "text-red-300" : "text-white/70";
const pointsDeltaLabel = (base: number, finalReward: number) => { const delta = finalReward - base; if (delta > 0) return `+${delta} durch Boni`; if (delta < 0) return `${delta} durch Dämpfung`; return "kein Auf- oder Abschlag"; };
function buildBehaviorHints(preview: RewardPreview | null, progress?: MissionProgress) { const avatarMultiplier = progress?.avatarMultiplier ?? preview?.avatarMultiplier ?? 1; const reasons = progress?.multiplierReasons ?? preview?.reasons ?? []; const hints: string[] = []; if (avatarMultiplier > 1) hints.push("Dein Avatar ist in gutem Zustand und verstärkt den Reward."); if (avatarMultiplier < 1) hints.push("Pflege deinen Avatar besser, damit dein Reward-Multiplikator steigt."); return [...reasons, ...hints].slice(0, 2); }

export default function MissionenPage() {
  const [brightness, setBrightness] = useState(100);
  const [activeTab, setActiveTab] = useState<MissionTab>("Tagesmissionen");
  const [selectedMissionId, setSelectedMissionId] = useState<string | null>("fallback-daily-steps");
  const [favoriteIds, setFavoriteIds] = useState<string[]>([]);
  const [historyIds, setHistoryIds] = useState<string[]>([]);
  const [completedMissionIds, setCompletedMissionIds] = useState<string[]>([]);
  const [missionProgressById, setMissionProgressById] = useState<Record<string, MissionProgress>>({});
  const [missions, setMissions] = useState<Mission[]>(fallbackMissions);
  const [message, setMessage] = useState("Bereit für KI-personalisierte Quests?");
  const [missionSourceMessage, setMissionSourceMessage] = useState("Fallback-Missionen aktiv – Firestore wird verbunden...");
  const [isSavingCompletion, setIsSavingCompletion] = useState(false);
  const [isGeneratingAi, setIsGeneratingAi] = useState(false);
  const [liveSteps, setLiveSteps] = useState(0);
  const [trackingActive, setTrackingActive] = useState(false);
  const [trackingSessionId, setTrackingSessionId] = useState<string | null>(null);
  const [rewardPreview, setRewardPreview] = useState<RewardPreview | null>(null);
  const stepCounterRef = useRef<ReturnType<typeof createBrowserStepCounter> | null>(null);

  useEffect(() => { const savedHistory = localStorage.getItem("wellfit-mission-history"); if (savedHistory) { try { setHistoryIds(JSON.parse(savedHistory)); } catch {} } }, []);
  useEffect(() => { const missionsQuery = query(collection(db, "missions"), where("active", "==", true), where("category", "==", "Tagesmissionen"), orderBy("sortOrder", "asc")); return onSnapshot(missionsQuery, (snapshot) => { const firestoreMissions = snapshot.docs.map((docSnap, index) => normalizeMission(docSnap.id, docSnap.data(), index)); if (firestoreMissions.length > 0) { setMissions(firestoreMissions); setSelectedMissionId((current) => current && firestoreMissions.some((mission) => mission.id === current) ? current : firestoreMissions[0].id); setMissionSourceMessage("Firestore Live · KI-Missionen und Reward Score aktiv"); } else { setMissions(fallbackMissions); setMissionSourceMessage("Noch keine aktiven Firestore-Missionen – Fallback aktiv"); } }, () => { setMissions(fallbackMissions); setMissionSourceMessage("Firestore Missionen nicht verfügbar – Fallback aktiv"); }); }, []);
  useEffect(() => { return onAuthStateChanged(auth, (firebaseUser) => { if (!firebaseUser) { setMissionProgressById({}); setCompletedMissionIds([]); setFavoriteIds([]); return; } const unsubscribeFavorites = listenUserFavorites(firebaseUser.uid, (favorites) => setFavoriteIds(favorites.map((favorite) => favorite.missionId))); const progressQuery = query(collection(db, "userMissionProgress"), where("userId", "==", firebaseUser.uid)); const unsubscribeProgress = onSnapshot(progressQuery, (snapshot) => { const progressMap: Record<string, MissionProgress> = {}; snapshot.docs.forEach((docSnap) => { const data = docSnap.data(); const missionId = String(data.missionId ?? ""); if (!missionId) return; progressMap[missionId] = { missionId, status: String(data.status ?? "open"), progressValue: Number(data.progressValue ?? 0), pointsGranted: Number(data.pointsGranted ?? 0), lockedRewardPoints: Number(data.lockedRewardPoints ?? 0), avatarMultiplier: Number(data.avatarMultiplier ?? 1), userEconomyMultiplier: Number(data.userEconomyMultiplier ?? 1), systemRewardRate: Number(data.systemRewardRate ?? data.rewardRateAtStart ?? 1), precisionFactor: Number(data.precisionFactor ?? 1), socialMultiplier: Number(data.socialMultiplier ?? 1), streakMultiplier: Number(data.streakMultiplier ?? 1), sponsorMultiplier: Number(data.sponsorMultiplier ?? 1), integrityMultiplier: Number(data.integrityMultiplier ?? 1), multiplierReasons: Array.isArray(data.multiplierReasons) ? data.multiplierReasons.map(String) : [], }; }); setMissionProgressById(progressMap); setCompletedMissionIds(Object.values(progressMap).filter((progress) => progress.status === "completed").map((progress) => progress.missionId)); }); return () => { unsubscribeFavorites(); unsubscribeProgress(); }; }); }, []);
  const filteredMissions = useMemo(() => activeTab === "Favoriten" ? missions.filter((mission) => favoriteIds.includes(mission.id)) : activeTab === "History" ? missions.filter((mission) => historyIds.includes(mission.id)) : missions.filter((mission) => mission.category === activeTab), [activeTab, favoriteIds, historyIds, missions]);
  useEffect(() => { if (filteredMissions.length === 0) { setSelectedMissionId(null); return; } if (!filteredMissions.some((mission) => mission.id === selectedMissionId)) setSelectedMissionId(filteredMissions[0].id); }, [filteredMissions, selectedMissionId]);
  const selectedMission = missions.find((mission) => mission.id === selectedMissionId) ?? null;
  const selectedProgress = selectedMission ? missionProgressById[selectedMission.id] : undefined;
  const targetSteps = selectedMission?.unit === "steps" ? selectedMission.targetValue ?? 6000 : 0;
  const stepProgressPercent = targetSteps > 0 ? Math.min(100, Math.round((liveSteps / targetSteps) * 100)) : selectedProgress?.progressValue ?? 34;
  const dailyMissions = missions.filter((mission) => mission.category === "Tagesmissionen").slice(0, 3);
  const favoriteMissions = missions.filter((mission) => favoriteIds.includes(mission.id));
  const fallbackFavorites = favoriteMissions.length ? favoriteMissions : missions.slice(0, 7);
  useEffect(() => { const calculatePreview = async () => { if (!selectedMission || !auth.currentUser) { setRewardPreview(null); return; } if (selectedProgress?.lockedRewardPoints) { setRewardPreview(null); return; } const multipliers = await calculateRewardMultipliers(auth.currentUser.uid); const systemRewardRate = getRewardRate(economyConfig.reserve, economyConfig.totalSupply); const score = calculateDynamicRewardScore({ baseReward: selectedMission.baseReward ?? selectedMission.reward, systemRewardRate, avatarMultiplier: multipliers.avatarMultiplier, userEconomyMultiplier: multipliers.userEconomyMultiplier }); setRewardPreview({ finalReward: score.finalReward, systemRewardRate, avatarMultiplier: multipliers.avatarMultiplier, userEconomyMultiplier: multipliers.userEconomyMultiplier, precisionFactor: score.precisionFactor, socialMultiplier: score.socialMultiplier, streakMultiplier: score.streakMultiplier, sponsorMultiplier: score.sponsorMultiplier, integrityMultiplier: score.integrityMultiplier, validationRisk: score.validationRisk, reasons: multipliers.reasons }); }; calculatePreview(); }, [selectedMission, selectedProgress?.lockedRewardPoints]);
  useEffect(() => { const firebaseUser = auth.currentUser; if (!firebaseUser || isGeneratingAi) return; const aiMissions = missions.filter((m) => m.source === "ai"); if (aiMissions.length >= 3) return; const generate = async () => { setIsGeneratingAi(true); try { for (let i = 0; i < 3 - aiMissions.length; i++) { await createAiMissionForCurrentUser({ level: 1, stepsToday: liveSteps, goal: "abnehmen", lastTargetValue: 10 + i, adaptiveLimit: 30, recentSuccessRate: 0.7, progressIndex: completedMissionIds.length, slotIndex: i }); } } catch (e) { console.error("KI Missionen konnten nicht erzeugt werden", e); } finally { setIsGeneratingAi(false); } }; generate(); }, [missions, liveSteps, completedMissionIds, isGeneratingAi]);
  const toggleFavorite = async (mission: Mission) => { const isFavorite = favoriteIds.includes(mission.id); try { await toggleMissionFavorite({ missionId: mission.id, title: mission.title, category: mission.category, description: mission.description, rewardPoints: mission.reward, rewardLabel: `+${mission.reward} WFT`, icon: missionIcon(mission.type), sourcePath: "/missionen/tagesmissionen", source: mission.source ?? "prefab" }, isFavorite); } catch (error) { setMessage(error instanceof Error ? error.message : "Favorit konnte nicht gespeichert werden."); } };
  const startSelectedMission = async () => { if (!selectedMission) return; try { const result = await startMission({ missionId: selectedMission.id, title: selectedMission.title, category: selectedMission.category, rewardPoints: selectedMission.baseReward ?? selectedMission.reward, missionKind: `${selectedMission.category}_${selectedMission.unit}_${selectedMission.title.toLowerCase().replace(/[^a-z0-9äöüß]+/gi, "_")}`, icon: missionIcon(selectedMission.type), proofType: selectedMission.unit === "steps" ? "steps" : "manual" }); setMessage(`Reward fixiert: +${result.lockedReward} WFT`); } catch (error) { setMessage(error instanceof Error ? error.message : "Mission konnte nicht gestartet werden."); } };
  const completeSelectedMission = async () => { if (!selectedMission) return; setIsSavingCompletion(true); try { const result = await completeMission({ missionId: selectedMission.id, title: selectedMission.title, category: selectedMission.category, rewardPoints: selectedMission.baseReward ?? selectedMission.reward, missionKind: `${selectedMission.category}_${selectedMission.unit}_${selectedMission.title.toLowerCase().replace(/[^a-z0-9äöüß]+/gi, "_")}`, icon: missionIcon(selectedMission.type), proofType: selectedMission.unit === "steps" ? "steps" : "manual" }); setMessage(`Mission abgeschlossen: +${result.pointsGranted} WFT`); const nextHistoryIds = Array.from(new Set([selectedMission.id, ...historyIds])); setHistoryIds(nextHistoryIds); localStorage.setItem("wellfit-mission-history", JSON.stringify(nextHistoryIds)); } catch (error) { setMessage(error instanceof Error ? error.message : "Mission konnte nicht gespeichert werden."); } finally { setIsSavingCompletion(false); } };
  const handleStartStepTracking = async () => { if (!selectedMission) return; await startSelectedMission(); const counter = createBrowserStepCounter({ onUpdate: (update) => { setLiveSteps(update.steps); if (selectedMission.unit === "steps" && selectedMission.targetValue && update.steps >= selectedMission.targetValue) completeSelectedMission(); }, onError: (msg) => setMessage(msg) }); const sessionId = await startTrackingSession({ source: "mobile", missionId: selectedMission.id, missionTitle: selectedMission.title, activityType: selectedMission.unit === "steps" ? "steps" : "manual" }); stepCounterRef.current = counter; setTrackingSessionId(sessionId); setLiveSteps(0); const started = await counter.start(); if (!started) { setTrackingSessionId(null); setMessage("Step Counter konnte nicht gestartet werden."); return; } setTrackingActive(true); setMessage("Tracking läuft. Reward wurde beim Start fixiert."); };
  const handleStopStepTracking = async () => { const countedSteps = stepCounterRef.current?.stop() ?? liveSteps; if (trackingSessionId) await finishTrackingSession({ sessionId: trackingSessionId, stepsAggregated: countedSteps, eventsCount: 1, notes: "Browser DeviceMotion Step Counter" }); setTrackingActive(false); setTrackingSessionId(null); };
  const handlePrimaryAction = async () => { if (selectedProgress?.status === "completed") return; if (!selectedProgress?.lockedRewardPoints) { if (selectedMission?.unit === "steps") return handleStartStepTracking(); return startSelectedMission(); } if (selectedMission?.unit === "steps" && trackingActive) return handleStopStepTracking(); return completeSelectedMission(); };
  const currentReward = selectedProgress?.lockedRewardPoints || rewardPreview?.finalReward || selectedMission?.reward || 0;
  const rewardBase = selectedMission?.baseReward ?? selectedMission?.reward ?? 0;
  const behaviorHints = buildBehaviorHints(rewardPreview, selectedProgress);
  const MissionTile = ({ mission, small = false }: { mission: Mission; small?: boolean }) => (<button onClick={() => setSelectedMissionId(mission.id)} className={`relative shrink-0 rounded-[10px] border bg-[#044b54]/95 text-left shadow-[0_4px_14px_rgba(0,0,0,0.25)] transition hover:bg-[#075d68] ${selectedMissionId === mission.id ? "border-yellow-400" : "border-cyan-300/25"} ${small ? "h-[145px] w-[120px] p-2" : "h-[118px] w-[130px] p-2"}`}><span onClick={(e) => { e.stopPropagation(); toggleFavorite(mission); }} className={`absolute right-2 top-1 text-xl ${favoriteIds.includes(mission.id) ? "text-yellow-400" : "text-yellow-400/45"}`}>★</span><div className="flex h-12 items-center justify-center text-3xl text-white/85">{missionIcon(mission.type)}</div><p className="line-clamp-2 text-center text-sm font-bold leading-tight text-white">{mission.title}</p><div className="absolute bottom-2 left-2 right-2 flex items-center gap-1 text-sm font-bold text-white"><Image src="/coin.png" alt="WFT" width={25} height={25} className="rounded-full" /><span>+ {mission.reward} WFT</span></div></button>);

  return (<main className="h-screen w-screen overflow-hidden text-white" style={{ background: `linear-gradient(to bottom right, rgba(0,170,190,${brightness / 100}), rgba(0,80,90,1))` }}><div className="flex h-full"><aside className="flex h-full w-[250px] flex-col border-r border-cyan-400/10 bg-[#042f35]/95 px-5 py-6"><div className="mb-8 flex justify-center"><Image src="/logo.png" alt="WellFit Logo" width={150} height={150} className="object-contain" priority /></div><nav className="space-y-2 text-[14px]"><Link href="/dashboard" className="block text-white/80">Dashboard</Link><div className="font-bold text-orange-400">Missionen</div><div className="text-white/80">Mein KI-Buddy</div><div className="text-white/80">Marktplatz</div><div className="text-white/80">Leaderboard</div><div className="text-white/80">Punkte-Shop</div><div className="text-white/80">Analytics & Stats</div></nav><div className="mt-5 border-t border-cyan-400/10 pt-4"><div className="mb-2 whitespace-nowrap text-base font-bold text-green-400">App aufs Handy laden</div><label className="mb-1 block text-lg">Helligkeit</label><input type="range" min="5" max="100" value={brightness} onChange={(e) => setBrightness(Number(e.target.value))} className="w-full" /><div className="mt-1 text-right text-sm text-white/70">{brightness}%</div></div><div className="mt-auto pt-4"><div className="space-y-2 text-[14px]"><Link href="/einstellungen" className="block text-white/80">Einstellungen</Link><Link href="/datenschutz" className="block text-white/80">Datenschutz</Link><Link href="/agb" className="block text-white/80">AGB</Link><Link href="/impressum" className="block text-white/80">Impressum</Link><Link href="/faq" className="block text-white/80">FAQ</Link><Link href="/hilfe" className="block text-white/80">Hilfe</Link></div><div className="mt-4 border-t border-cyan-400/10 pt-3"><button className="text-[14px] font-bold text-red-400 hover:text-red-300">Abmelden</button></div></div></aside><section className="relative flex h-full flex-1 flex-col overflow-hidden px-7 py-5 pb-0"><div className="mb-4 flex justify-between"><div><h1 className="text-5xl font-extrabold leading-none">Tagesmissionen</h1><p className="mt-1 text-lg text-cyan-100/90">{message}</p><p className="mt-1 text-xs text-cyan-100/65">{missionSourceMessage}</p></div><div className="flex items-center gap-2"><button onClick={selectedMission?.unit === "steps" ? handleStartStepTracking : startSelectedMission} className="rounded-[16px] bg-orange-500 px-5 py-3 text-sm font-bold">Tracker starten</button><div className="rounded-full bg-[#073b44] px-4 py-2 text-sm">Flammi LVL 1</div></div></div><div className="mb-4 flex justify-center"><div className="flex items-center gap-5 rounded-full border border-white/10 bg-[#0b6d79]/35 px-5 py-3 shadow-[0_8px_24px_rgba(0,0,0,0.18)] backdrop-blur-sm">{tabs.map((tab) => activeTab === tab ? <div key={tab} className="relative pb-1 text-base font-semibold text-orange-400">{tab}<span className="absolute left-0 right-0 -bottom-2 h-[2px] rounded-full bg-orange-400" /></div> : <Link key={tab} href={tabHref(tab)} onClick={() => setActiveTab(tab)} className="pb-1 text-base text-white/85 hover:text-white">{tab}</Link>)}</div></div><div className="grid min-h-0 flex-1 grid-cols-[2.25fr_0.95fr] gap-5 overflow-hidden pb-20"><div className="min-h-0 overflow-y-auto pr-2"><section className="mb-4 rounded-[8px] bg-[#00606b]/75 px-4 py-3"><h2 className="mb-2 w-fit border-b border-white/70 text-2xl font-extrabold tracking-wide">Heute auswählen</h2><div className="flex gap-3 overflow-x-auto pb-1">{dailyMissions.map((mission) => <MissionTile key={mission.id} mission={mission} />)}</div></section><section className="mb-4 rounded-[8px] bg-[#00606b]/75 px-4 py-3"><h2 className="mb-2 w-fit border-b border-white/70 text-2xl font-extrabold tracking-wide">Favoriten</h2><div className="flex gap-3 overflow-x-auto pb-1">{fallbackFavorites.map((mission) => <MissionTile key={`fav-${mission.id}`} mission={mission} />)}</div></section><section className="rounded-[8px] bg-[#00606b]/75 px-4 py-3"><h2 className="mb-3 w-fit border-b border-white/70 text-3xl font-extrabold tracking-wide">Tagesmissionen</h2><div className="grid grid-cols-[repeat(auto-fill,minmax(115px,1fr))] gap-3">{filteredMissions.map((mission) => <MissionTile key={mission.id} mission={mission} small />)}</div></section></div><aside className="min-h-0 overflow-y-auto rounded-[6px] bg-[#003d46]/95 p-6 shadow-[0_12px_30px_rgba(0,0,0,0.2)]">{selectedMission ? (<><div className="mb-6 flex items-start justify-between"><h2 className="text-2xl font-extrabold tracking-wide">Missionsdetails</h2><button onClick={() => toggleFavorite(selectedMission)} className={`text-5xl leading-none ${favoriteIds.includes(selectedMission.id) ? "text-yellow-400" : "text-yellow-400/50"}`}>★</button></div><div className="flex justify-center py-2 text-7xl text-cyan-300">{missionIcon(selectedMission.type)}</div><h3 className="mt-4 text-center text-3xl font-extrabold leading-tight">{selectedMission.title}</h3><p className="mt-4 text-center text-xl leading-tight text-white/90">{selectedMission.description}</p><div className="mt-4"><div className="mb-1 flex justify-end text-2xl font-extrabold">{stepProgressPercent}%</div><div className="h-7 overflow-hidden rounded bg-[#062e34]"><div className="h-full rounded bg-cyan-300" style={{ width: `${stepProgressPercent}%` }} /></div></div><div className="mt-4 flex items-center justify-between text-lg"><span>Level-Empfehlung</span><span className="font-bold">4+</span></div><div className="mt-2 flex items-center gap-3 text-xl font-bold"><Image src="/coin.png" alt="WFT" width={38} height={38} className="rounded-full" /><span>+ {currentReward} WFT Belohnung</span></div><div className="mt-3 rounded-xl border border-cyan-400/20 bg-cyan-400/10 p-3 text-xs text-cyan-50/90"><p>{behaviorHints[0] ?? pointsDeltaLabel(rewardBase, currentReward)}</p><p className="mt-1">System {formatMultiplier(selectedProgress?.systemRewardRate ?? rewardPreview?.systemRewardRate)} · Avatar <span className={multiplierColor(selectedProgress?.avatarMultiplier ?? rewardPreview?.avatarMultiplier)}>{formatMultiplier(selectedProgress?.avatarMultiplier ?? rewardPreview?.avatarMultiplier)}</span></p></div><button onClick={handlePrimaryAction} disabled={isSavingCompletion || selectedProgress?.status === "completed"} className={`mt-5 w-full rounded-[16px] px-4 py-3 text-lg font-extrabold ${selectedProgress?.status === "completed" ? "bg-green-700/70" : "bg-blue-600 hover:bg-blue-700"}`}>{selectedProgress?.status === "completed" ? "Mission erledigt" : selectedProgress?.lockedRewardPoints ? "Mission abschließen" : "Mission starten"}</button></>) : <div className="flex h-full items-center justify-center text-white/40">Wähle eine Mission</div>}</aside></div><div className="absolute bottom-0 left-0 right-0 flex items-center justify-between border-t border-cyan-400/10 bg-[#062f35]/95 px-5 py-3"><div className="flex items-center gap-3"><div className="min-w-[150px] rounded-xl border border-cyan-400/10 bg-[#041f24] px-3 py-2"><p className="text-[10px] uppercase text-white/50">Letzter Login: Heute 9:43</p><p className="mt-1 text-sm font-semibold text-white">3 WFT erhalten</p></div><div className="min-w-[150px] rounded-xl border border-yellow-500/60 bg-[#041f24] px-3 py-2 text-center"><p className="text-[10px] uppercase text-white/50">Reward Score</p><p className="mt-1 text-lg font-bold text-white">{currentReward}</p></div><div className="min-w-[150px] rounded-xl border border-cyan-400/10 bg-[#041f24] px-3 py-2 text-center"><p className="text-[10px] uppercase text-white/50">Live Schritte</p><p className="mt-1 text-sm font-semibold text-white/70">{liveSteps}</p></div><div className="min-w-[170px] rounded-xl border border-yellow-500/40 bg-[#0a3d46] px-3 py-2 text-center"><p className="text-sm font-semibold text-yellow-400">Erledigt: {completedMissionIds.length}</p></div></div><div className="flex items-center gap-3 text-xl text-white/80"><span>f</span><span>𝕏</span><span>in</span></div></div></section></div></main>);
}
