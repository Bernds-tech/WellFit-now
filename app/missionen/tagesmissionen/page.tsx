"use client";

import Image from "next/image";
import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import type { User } from "@/types/user";
import { completeMission } from "@/lib/missionCompletion";
import { finishTrackingSession, startTrackingSession } from "@/lib/tracking";
import { createBrowserStepCounter } from "@/lib/stepCounter";
import { auth, db } from "@/lib/firebase";
import { onAuthStateChanged } from "firebase/auth";
import { collection, onSnapshot, orderBy, query, where } from "firebase/firestore";

type MissionTab = "Tagesmissionen" | "Wochenmissionen" | "Abenteuer" | "Challenge" | "Wettkämpfe" | "Favoriten" | "History";
type Mission = { id: string; numericId: number; title: string; reward: number; category: MissionTab; difficulty: "Leicht" | "Mittel" | "Schwer"; description: string; duration: string; type: "Bewegung" | "Ernährung" | "Workout" | "Community" | "Abenteuer"; targetValue?: number; unit?: "steps" | "minutes" | "questions" | "checkin" | string; aiPersonalized?: boolean; source?: "firestore" | "fallback" | "ai"; };
type MissionProgress = { missionId: string; status: "open" | "active" | "completed" | "expired" | string; progressValue: number; pointsGranted?: number; };

const tabs: MissionTab[] = ["Tagesmissionen", "Wochenmissionen", "Abenteuer", "Challenge", "Wettkämpfe", "Favoriten", "History"];
const tabHref = (tab: MissionTab) => tab === "Tagesmissionen" ? "/missionen/tagesmissionen" : tab === "Wochenmissionen" ? "/missionen/wochenmissionen" : tab === "Abenteuer" ? "/missionen/abenteuer" : tab === "Challenge" ? "/missionen/challenge" : tab === "Wettkämpfe" ? "/missionen/wettkaempfe" : tab === "Favoriten" ? "/missionen/favoriten" : "/missionen/history";
const missionIcon = (type: Mission["type"]) => type === "Bewegung" ? "👣" : type === "Ernährung" ? "💧" : type === "Workout" ? "🔥" : type === "Community" ? "👥" : "🗺️";

const fallbackMissions: Mission[] = [
  { id: "fallback-daily-steps", numericId: 1, title: "Tägliche Schritte", reward: 25, category: "Tagesmissionen", difficulty: "Leicht", description: "Erreiche heute 5.000 Schritte und halte deinen Bewegungsfluss aktiv.", duration: "1 Tag", type: "Bewegung", targetValue: 5000, unit: "steps", aiPersonalized: true, source: "fallback" },
  { id: "fallback-daily-water", numericId: 2, title: "Wasser trinken", reward: 10, category: "Tagesmissionen", difficulty: "Leicht", description: "Trinke heute mindestens 2 Liter Wasser für deinen Energiehaushalt.", duration: "1 Tag", type: "Ernährung", targetValue: 1, unit: "checkin", aiPersonalized: true, source: "fallback" },
  { id: "fallback-daily-workout", numericId: 3, title: "Workout Boost", reward: 40, category: "Tagesmissionen", difficulty: "Mittel", description: "Absolviere ein 20-minütiges Workout und verdiene Extra-Punkte.", duration: "20 Minuten", type: "Workout", targetValue: 20, unit: "minutes", aiPersonalized: true, source: "fallback" },
];

const normalizeMission = (docId: string, data: any, index: number): Mission => ({ id: docId, numericId: typeof data.numericId === "number" ? data.numericId : index + 1, title: String(data.title ?? "KI Mission"), reward: Number(data.pointsReward ?? data.reward ?? 0), category: String(data.category ?? data.displayCategory ?? "Tagesmissionen") as MissionTab, difficulty: (data.difficulty === "Schwer" || data.difficulty === "Mittel" || data.difficulty === "Leicht") ? data.difficulty : "Leicht", description: String(data.description ?? "Diese Mission wurde dynamisch vorbereitet."), duration: String(data.duration ?? data.timeWindow ?? "1 Tag"), type: (data.type === "Ernährung" || data.type === "Workout" || data.type === "Community" || data.type === "Abenteuer" || data.type === "Bewegung") ? data.type : "Bewegung", targetValue: Number(data.targetValue ?? (data.unit === "steps" ? 5000 : 1)), unit: String(data.unit ?? (data.type === "Bewegung" ? "steps" : "checkin")), aiPersonalized: Boolean(data.aiPersonalized ?? data.generatedByAi ?? true), source: data.generatedByAi ? "ai" : "firestore" });

export default function MissionenPage() {
  const [brightness, setBrightness] = useState(100);
  const [activeTab, setActiveTab] = useState<MissionTab>("Tagesmissionen");
  const [selectedMissionId, setSelectedMissionId] = useState<string | null>("fallback-daily-steps");
  const [favoriteIds, setFavoriteIds] = useState<string[]>([]);
  const [historyIds, setHistoryIds] = useState<string[]>([]);
  const [activeMissionIds, setActiveMissionIds] = useState<string[]>([]);
  const [completedMissionIds, setCompletedMissionIds] = useState<string[]>([]);
  const [missionProgressById, setMissionProgressById] = useState<Record<string, MissionProgress>>({});
  const [missions, setMissions] = useState<Mission[]>(fallbackMissions);
  const [missionSourceMessage, setMissionSourceMessage] = useState("Fallback-Missionen aktiv – Firestore wird verbunden...");
  const [user, setUser] = useState<User | null>(null);
  const [message, setMessage] = useState("Bereit für KI-personalisierte Quests?");
  const [isSavingCompletion, setIsSavingCompletion] = useState(false);
  const [liveSteps, setLiveSteps] = useState(0);
  const [trackingActive, setTrackingActive] = useState(false);
  const [trackingSessionId, setTrackingSessionId] = useState<string | null>(null);
  const stepCounterRef = useRef<ReturnType<typeof createBrowserStepCounter> | null>(null);

  useEffect(() => { const savedUser = localStorage.getItem("wellfit-user"); const savedFavorites = localStorage.getItem("wellfit-favorite-missions"); const savedHistory = localStorage.getItem("wellfit-mission-history"); if (savedUser) { try { setUser(JSON.parse(savedUser)); } catch (error) { console.error("Fehler beim Laden des Users", error); } } if (savedFavorites) { try { setFavoriteIds(JSON.parse(savedFavorites)); } catch (error) { console.error("Fehler beim Laden der Favoriten", error); } } if (savedHistory) { try { setHistoryIds(JSON.parse(savedHistory)); } catch (error) { console.error("Fehler beim Laden der History", error); } } }, []);

  useEffect(() => { const missionsQuery = query(collection(db, "missions"), where("active", "==", true), where("category", "==", "Tagesmissionen"), orderBy("sortOrder", "asc")); const unsubscribe = onSnapshot(missionsQuery, (snapshot) => { const firestoreMissions = snapshot.docs.map((docSnap, index) => normalizeMission(docSnap.id, docSnap.data(), index)); if (firestoreMissions.length > 0) { setMissions(firestoreMissions); setSelectedMissionId((current) => current && firestoreMissions.some((mission) => mission.id === current) ? current : firestoreMissions[0].id); setMissionSourceMessage("Firestore Live · KI-Missionen skalierbar vorbereitet"); return; } setMissions(fallbackMissions); setSelectedMissionId((current) => current ?? fallbackMissions[0].id); setMissionSourceMessage("Noch keine aktiven Firestore-Missionen – Fallback aktiv"); }, (error) => { console.error("Missionen konnten nicht live geladen werden", error); setMissions(fallbackMissions); setMissionSourceMessage("Firestore Missionen nicht verfügbar – Fallback aktiv"); }); return () => unsubscribe(); }, []);

  useEffect(() => { const unsubscribeAuth = onAuthStateChanged(auth, (firebaseUser) => { if (!firebaseUser) { setMissionProgressById({}); setCompletedMissionIds([]); setActiveMissionIds([]); return; } const progressQuery = query(collection(db, "userMissionProgress"), where("userId", "==", firebaseUser.uid)); const unsubscribeProgress = onSnapshot(progressQuery, (snapshot) => { const progressMap: Record<string, MissionProgress> = {}; snapshot.docs.forEach((docSnap) => { const data = docSnap.data(); const missionId = String(data.missionId ?? ""); if (!missionId) return; progressMap[missionId] = { missionId, status: String(data.status ?? "open"), progressValue: Number(data.progressValue ?? 0), pointsGranted: typeof data.pointsGranted === "number" ? data.pointsGranted : undefined }; }); setMissionProgressById(progressMap); setCompletedMissionIds(Object.values(progressMap).filter((progress) => progress.status === "completed").map((progress) => progress.missionId)); setActiveMissionIds(Object.values(progressMap).filter((progress) => progress.status === "active").map((progress) => progress.missionId)); setMessage("Mission Progress live aus Firestore geladen."); }, (error) => { console.error("Mission Progress konnte nicht live geladen werden", error); setMessage("Mission Progress konnte nicht live geladen werden."); }); return unsubscribeProgress; }); return () => unsubscribeAuth(); }, []);

  const filteredMissions = useMemo(() => activeTab === "Favoriten" ? missions.filter((mission) => favoriteIds.includes(mission.id)) : activeTab === "History" ? missions.filter((mission) => historyIds.includes(mission.id)) : missions.filter((mission) => mission.category === activeTab), [activeTab, favoriteIds, historyIds, missions]);
  useEffect(() => { if (filteredMissions.length === 0) { setSelectedMissionId(null); return; } if (!filteredMissions.some((mission) => mission.id === selectedMissionId)) setSelectedMissionId(filteredMissions[0].id); }, [filteredMissions, selectedMissionId]);

  const selectedMission = missions.find((mission) => mission.id === selectedMissionId) ?? null;
  const selectedProgress = selectedMission ? missionProgressById[selectedMission.id] : undefined;
  const selectedProgressValue = selectedProgress?.progressValue ?? 0;
  const targetSteps = selectedMission?.unit === "steps" ? selectedMission.targetValue ?? 5000 : 0;
  const stepProgressPercent = targetSteps > 0 ? Math.min(100, Math.round((liveSteps / targetSteps) * 100)) : selectedProgressValue;
  const dailyMissions = missions.filter((mission) => mission.category === "Tagesmissionen");
  const favoriteMissions = missions.filter((mission) => favoriteIds.includes(mission.id));

  const toggleFavorite = (missionId: string) => { const updated = favoriteIds.includes(missionId) ? favoriteIds.filter((id) => id !== missionId) : [...favoriteIds, missionId]; setFavoriteIds(updated); localStorage.setItem("wellfit-favorite-missions", JSON.stringify(updated)); };
  const completeSelectedMission = async () => { if (!selectedMission) { setMessage("Bitte zuerst eine Mission auswählen."); return; } if (completedMissionIds.includes(selectedMission.id)) { setMessage(`Mission bereits erledigt: ${selectedMission.title}`); return; } setIsSavingCompletion(true); try { await completeMission({ missionId: selectedMission.id, title: selectedMission.title, category: selectedMission.category, rewardPoints: selectedMission.reward, rewardLabel: `+${selectedMission.reward} WFT`, icon: missionIcon(selectedMission.type), proofType: selectedMission.unit === "steps" ? "steps" : selectedMission.type === "Workout" ? "event" : "manual" }); const updatedHistory = historyIds.includes(selectedMission.id) ? historyIds : [selectedMission.id, ...historyIds]; setHistoryIds(updatedHistory); localStorage.setItem("wellfit-mission-history", JSON.stringify(updatedHistory)); setMessage(`Mission abgeschlossen und in Firestore gespeichert: ${selectedMission.title} (+${selectedMission.reward} WFT)`); } catch (error) { console.error("Mission konnte nicht abgeschlossen werden", error); setMessage(error instanceof Error ? error.message : "Mission konnte nicht in Firestore gespeichert werden."); } finally { setIsSavingCompletion(false); } };
  const handleStartStepTracking = async () => { if (!selectedMission) { setMessage("Bitte zuerst eine Mission auswählen."); return; } const counter = createBrowserStepCounter({ onUpdate: (update) => { setLiveSteps(update.steps); if (selectedMission.unit === "steps" && selectedMission.targetValue && update.steps >= selectedMission.targetValue && !completedMissionIds.includes(selectedMission.id)) { completeSelectedMission(); } }, onError: (msg) => setMessage(msg) }); const sessionId = await startTrackingSession({ source: "mobile", missionId: selectedMission.id, missionTitle: selectedMission.title, activityType: selectedMission.unit === "steps" ? "steps" : "manual" }); stepCounterRef.current = counter; setTrackingSessionId(sessionId); setLiveSteps(0); const started = await counter.start(); if (!started) { setTrackingSessionId(null); setMessage("Step Counter konnte nicht gestartet werden."); return; } setTrackingActive(true); setMessage("Echte Schritte werden gezählt. Sobald das Ziel erreicht ist, wird die Mission automatisch abgeschlossen."); };
  const handleStopStepTracking = async () => { const countedSteps = stepCounterRef.current?.stop() ?? liveSteps; if (trackingSessionId) await finishTrackingSession({ sessionId: trackingSessionId, stepsAggregated: countedSteps, eventsCount: 1, notes: "Browser DeviceMotion Step Counter" }); setTrackingActive(false); setTrackingSessionId(null); setMessage(`Tracking beendet und gespeichert: ${countedSteps} Schritte.`); };
  const handlePrimaryAction = async () => { if (selectedMission?.unit === "steps" && !trackingActive) return handleStartStepTracking(); if (selectedMission?.unit === "steps" && trackingActive) return handleStopStepTracking(); return completeSelectedMission(); };

  const missionCardClass = "cursor-pointer rounded-[20px] border p-4 transition";

  return (<main className="h-screen w-screen overflow-hidden text-white" style={{ background: `linear-gradient(to bottom right, rgba(0,170,190,${brightness / 100}), rgba(0,80,90,1))` }}><div className="flex h-full"><aside className="flex h-full w-[250px] flex-col border-r border-cyan-400/10 bg-[#042f35]/95 px-5 py-6"><div className="mb-8 flex justify-center"><Image src="/logo.png" alt="WellFit Logo" width={150} height={150} className="object-contain" priority /></div><nav className="space-y-2 text-[14px]"><Link href="/dashboard" className="block text-white/80">Dashboard</Link><div className="font-bold text-orange-400">Missionen</div><div className="text-white/80">Mein KI-Buddy</div><div className="text-white/80">Marktplatz</div><div className="text-white/80">Leaderboard</div><div className="text-white/80">Punkte-Shop</div><div className="text-white/80">Analytics & Stats</div></nav><div className="mt-5 border-t border-cyan-400/10 pt-4"><div className="mb-2 whitespace-nowrap text-base font-bold text-green-400">App aufs Handy laden</div><label className="mb-1 block text-lg">Helligkeit</label><input type="range" min="5" max="100" value={brightness} onChange={(e) => setBrightness(Number(e.target.value))} className="w-full" /><div className="mt-1 text-right text-sm text-white/70">{brightness}%</div></div><div className="mt-auto pt-4"><div className="space-y-2 text-[14px]"><Link href="/einstellungen" className="block text-white/80">Einstellungen</Link><Link href="/datenschutz" className="block text-white/80">Datenschutz</Link><Link href="/agb" className="block text-white/80">AGB</Link><Link href="/impressum" className="block text-white/80">Impressum</Link><Link href="/faq" className="block text-white/80">FAQ</Link><Link href="/hilfe" className="block text-white/80">Hilfe</Link></div><div className="mt-4 border-t border-cyan-400/10 pt-3"><button className="text-[14px] font-bold text-red-400 hover:text-red-300">Abmelden</button></div></div></aside><section className="relative flex h-full flex-1 flex-col overflow-hidden px-7 py-5 pb-0"><div className="mb-4 flex justify-between"><div><h1 className="text-5xl font-extrabold leading-none">Missionen</h1><p className="mt-1 text-lg text-cyan-100/90">{message}</p><p className="mt-1 text-xs text-cyan-100/65">{missionSourceMessage}</p></div><div className="flex items-center gap-2"><button className="rounded-full border border-cyan-300/20 bg-[#0a6b78]/20 px-4 py-2 text-sm text-white/90">Synchron</button><button onClick={trackingActive ? handleStopStepTracking : handleStartStepTracking} className="rounded-[16px] bg-orange-500 px-5 py-3 text-sm font-bold">{trackingActive ? "Tracking stoppen" : "Tracker starten"}</button><div className="rounded-full bg-[#073b44] px-4 py-2 text-sm">Flammi LVL 1</div></div></div><div className="mb-4 flex justify-center"><div className="flex items-center gap-5 rounded-full border border-white/10 bg-[#0b6d79]/35 px-5 py-3 shadow-[0_8px_24px_rgba(0,0,0,0.18)] backdrop-blur-sm">{tabs.map((tab) => (<Link key={tab} href={tabHref(tab)} onClick={() => setActiveTab(tab)} className={`relative pb-1 text-base transition ${activeTab === tab ? "font-semibold text-orange-400" : "text-white/85 hover:text-white"}`}>{tab}{activeTab === tab && <span className="absolute left-0 right-0 -bottom-2 h-[2px] rounded-full bg-orange-400" />}</Link>))}</div></div><div className="grid min-h-0 flex-1 grid-cols-[2fr_1fr] gap-4 overflow-hidden pb-20"><div className="min-h-0 space-y-5 overflow-y-auto pr-3 pb-5"><div><h2 className="mb-3 text-xl font-bold text-cyan-300">KI-Tagesziele ({Math.min(dailyMissions.length, 3)}/3)</h2><div className="flex gap-3">{dailyMissions.slice(0, 1).map((mission) => (<div key={mission.id} onClick={() => setSelectedMissionId(mission.id)} className={`flex h-[120px] w-[120px] cursor-pointer flex-col justify-between rounded-[20px] border p-3 transition ${selectedMissionId === mission.id ? "border-yellow-400 bg-[#07505c]" : "border-cyan-300/10 bg-[#053841]/90 hover:bg-[#07505c]"}`}><div className="flex items-start justify-between"><div className="text-2xl">{missionIcon(mission.type)}</div><button onClick={(e) => { e.stopPropagation(); toggleFavorite(mission.id); }} className={`text-lg ${favoriteIds.includes(mission.id) ? "text-yellow-400" : "text-white/30"}`}>★</button></div><div><p className="text-sm font-bold">{mission.title}</p><p className="mt-1 text-xs font-semibold text-orange-400">+ {mission.reward} WFT</p></div></div>))}{[1, 2].map((slot) => (<div key={slot} className="flex h-[120px] w-[120px] flex-col items-center justify-center rounded-[20px] border border-dashed border-white/25 bg-white/5 text-white/35"><div className="text-2xl">⊕</div><p className="mt-2 text-sm">KI-Slot</p></div>))}</div></div><div><h2 className="mb-3 text-xl font-bold text-yellow-400">Deine Favoriten</h2><div className="flex gap-3 overflow-x-auto pb-2">{favoriteMissions.length > 0 ? favoriteMissions.map((mission) => (<div key={mission.id} onClick={() => setSelectedMissionId(mission.id)} className={`flex h-[135px] min-w-[120px] cursor-pointer flex-col justify-between rounded-[20px] border p-3 transition ${selectedMissionId === mission.id ? "border-yellow-400 bg-[#07505c]" : "border-cyan-300/10 bg-[#053841]/90 hover:bg-[#07505c]"}`}><div className="flex items-start justify-between"><div className="text-2xl">{missionIcon(mission.type)}</div><span className="text-lg text-yellow-400">★</span></div><div><p className="text-sm font-bold">{mission.title}</p><p className="mt-1 text-xs font-semibold text-orange-400">+ {mission.reward} WFT</p></div></div>)) : <div className="rounded-xl border border-dashed border-white/20 p-4 text-sm text-white/40">Noch keine Favoriten gespeichert</div>}</div></div><div><h2 className="mb-3 text-xl font-bold text-white">Missions-Pool</h2><div className="grid grid-cols-4 gap-3">{filteredMissions.length > 0 ? filteredMissions.map((mission) => { const isFavorite = favoriteIds.includes(mission.id); const progress = missionProgressById[mission.id]; const isCompleted = progress?.status === "completed"; return (<div key={mission.id} onClick={() => setSelectedMissionId(mission.id)} className={`${missionCardClass} ${selectedMissionId === mission.id ? "border-yellow-400 bg-[#07505c]" : "border-cyan-300/10 bg-[#053841]/90 hover:bg-[#07505c]"}`}><div className="mb-2 flex items-start justify-between gap-2"><div className="text-2xl">{missionIcon(mission.type)}</div><button onClick={(e) => { e.stopPropagation(); toggleFavorite(mission.id); }} className={`text-lg ${isFavorite ? "text-yellow-400" : "text-white/30"}`}>★</button></div><p className="text-base font-bold">{mission.title}</p><p className="mt-1 text-xs text-white/60">{mission.type} · {mission.difficulty}</p><p className="mt-3 text-sm font-semibold text-orange-400">+ {mission.reward} WFT</p>{isCompleted && <p className="mt-2 text-xs font-semibold text-green-300">Erledigt</p>}</div>); }) : <div className="col-span-4 rounded-xl border border-dashed border-white/20 p-4 text-sm text-white/40">Keine Missionen in diesem Bereich gefunden.</div>}</div></div></div><div className="min-h-0 overflow-y-auto rounded-[24px] bg-[#053841]/90 p-5 shadow-[0_12px_30px_rgba(0,0,0,0.18)]">{selectedMission ? (<><div className="mb-4 flex items-start justify-between gap-3"><h2 className="text-2xl font-bold text-white">Missionsdetails</h2><button onClick={() => toggleFavorite(selectedMission.id)} className={`text-2xl ${favoriteIds.includes(selectedMission.id) ? "text-yellow-400" : "text-white/30"}`}>★</button></div><div className="mb-3 flex justify-center text-4xl">{missionIcon(selectedMission.type)}</div><h3 className="text-center text-2xl font-bold text-white">{selectedMission.title}</h3><div className="mt-3 rounded-lg border border-fuchsia-500/40 bg-fuchsia-500/10 px-3 py-2 text-center text-sm font-semibold text-fuchsia-300">{selectedMission.source === "fallback" ? "Fallback" : selectedMission.source === "ai" ? "KI-generiert" : "Firestore"} · {selectedMission.type}</div><p className="mt-4 text-center text-sm leading-relaxed text-white/80">{selectedMission.description}</p><div className="mt-4"><div className="mb-2 flex items-center justify-between text-sm text-white/80"><span>{selectedMission.unit === "steps" ? "Live Schritte" : "Fortschritt"}</span><span>{selectedMission.unit === "steps" ? `${liveSteps}/${targetSteps}` : selectedProgress?.status === "completed" ? "100%" : `${selectedProgressValue}%`}</span></div><div className="h-2 w-full overflow-hidden rounded-full bg-black/25"><div className="h-full rounded-full bg-cyan-300" style={{ width: selectedProgress?.status === "completed" ? "100%" : `${selectedMission.unit === "steps" ? stepProgressPercent : selectedProgressValue}%` }} /></div><p className="mt-2 text-xs text-white/70">Fortschritt: {selectedProgress?.status === "completed" ? "abgeschlossen" : selectedMission.unit === "steps" ? `${stepProgressPercent}% durch echte Schritte` : `${selectedProgressValue} / ${selectedMission.duration}`}</p></div><div className="mt-4 rounded-xl border border-yellow-500/30 bg-yellow-500/10 px-4 py-3"><div className="flex items-center justify-between text-base"><span className="font-semibold text-white/90">Belohnung:</span><span className="font-bold text-yellow-400">+ {selectedMission.reward} WFT</span></div></div><div className="mt-4 space-y-2 text-sm text-white/80"><p><span className="font-semibold text-white">Dauer:</span> {selectedMission.duration}</p><p><span className="font-semibold text-white">Schwierigkeit:</span> {selectedMission.difficulty}</p><p><span className="font-semibold text-white">Typ:</span> {selectedMission.type}</p></div><button onClick={handlePrimaryAction} disabled={isSavingCompletion || selectedProgress?.status === "completed"} className={`mt-5 w-full rounded-xl px-4 py-3 text-base font-bold text-white transition ${selectedProgress?.status === "completed" ? "cursor-not-allowed bg-green-700/70" : "bg-blue-600 hover:bg-blue-700"}`}>{selectedProgress?.status === "completed" ? "Mission erledigt" : isSavingCompletion ? "Speichere in Firestore..." : selectedMission.unit === "steps" ? trackingActive ? "Tracking speichern" : "Echte Schritte starten" : "Mission abschließen"}</button></>) : <div className="flex h-full items-center justify-center text-white/40">Wähle eine Mission</div>}</div></div><div className="absolute bottom-0 left-0 right-0 flex items-center justify-between border-t border-cyan-400/10 bg-[#062f35]/95 px-5 py-3"><div className="flex items-center gap-3"><div className="min-w-[150px] rounded-xl border border-cyan-400/10 bg-[#041f24] px-3 py-2"><p className="text-[10px] uppercase text-white/50">Speicher</p><p className="mt-1 text-sm font-semibold text-white">Firestore Live</p></div><div className="min-w-[150px] rounded-xl border border-yellow-500/60 bg-[#041f24] px-3 py-2 text-center"><p className="text-[10px] uppercase text-white/50">Live Schritte</p><p className="mt-1 text-lg font-bold text-white">{liveSteps}</p></div><div className="min-w-[150px] rounded-xl border border-cyan-400/10 bg-[#041f24] px-3 py-2 text-center"><p className="text-[10px] uppercase text-white/50">Erledigt</p><p className="mt-1 text-lg font-bold text-white">{completedMissionIds.length}</p></div><div className="min-w-[170px] rounded-xl border border-yellow-500/40 bg-[#0a3d46] px-3 py-2 text-center"><p className="text-sm font-semibold text-yellow-400">Steps → Completion</p></div></div><div className="flex items-center gap-3 text-xl text-white/80"><span>f</span><span>𝕏</span><span>in</span></div></div></section></div></main>);
}
