# FanMind cleanup record

Date: 2026-07-20

Removed from the active WellFit repository scope:

- `.github/workflows/deploy-fanmind.yml`
- WellFit issue #305 was closed as `not_planned` because it belongs to FanMind

The repository is now explicitly documented as GitHub-only for current WellFit development. No active WellFit server, SSH target, PM2 process, Firebase production deployment, or cross-product deployment workflow is authorized by this cleanup.

Historical commits and closed pull requests remain in Git history for traceability; they are not active WellFit product code.
