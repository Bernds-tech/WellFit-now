# WELLFIT – J8.3 AR Raetselrallye Reward Algorithmus Addendum

Stand: 2026-04-28
Status: Ergaenzung zu `J - NÄCHSTE EMPFOHLENE ARBEIT` und `G1 - INTERNE PUNKTEOEKONOMIE VOR BLOCKCHAIN`

## Zweck

Dieses Addendum dokumentiert die wichtige Produkt- und Architekturentscheidung fuer AR-Buddy-Fragen, Objekterkennung, Standort-Raetsel und Raetselrallyes.

## Verbindliche Entscheidung

AR-Buddy, Unity, Kamera-KI, Mobile UI und Buddy-KI duerfen keine Punktehoehe festlegen.

Sie duerfen nur erzeugen:

```txt
AR-Event
Objekt-/Kontext-Evidence
Fragevorschlag
Antwortauswahl
Buddy-Reaktion
```

Die Punktehoehe wird ausschliesslich serverseitig durch den bestehenden bzw. geplanten Punkte-/Reward-Algorithmus berechnet.

## Ortsnaehe als Produktregel

AR-Raetselrallyes muessen in der aktuellen Umgebung des Nutzers stattfinden.

```txt
Keine langen Pflichtwege
Keine Stunde Fahrt
Start im Nahbereich: Waldrand, Park, Spielplatz, Museum, Stadtplatz, Burg, Zoo, Schulhof usw.
Radius alters-, kontext- und sicherheitssensibel
```

Die KI darf Rallyes und Aufgaben selbststaendig vorschlagen, aber Server/Policy begrenzt Radius, Sicherheit, Wiederholung, Evidence und Reward.

## Bezug zu G1

G1 bleibt verbindlich:

```txt
Punkte zuerst
Server-Ledger zuerst
Anti-Cheat zuerst
Beta-Test zuerst
Blockchain danach
```

Die geplante 25-Mrd.-WFT-Logik bleibt aktuell interne Punkte-/Economy-Simulation.

## Zielablauf fuer AR-Fragen

```txt
1. Kamera/KI erkennt Objekt oder Standortkontext
2. Buddy formuliert Aufgabe oder Frage
3. Nutzer waehlt Antwort
4. App erzeugt arQuestionAnswered Evidence
5. Backend prueft Antwort und Kontext
6. Question Memory prueft Wiederholung/Farming
7. Reward Policy berechnet erlaubte Punkte/XP
8. Server-Ledger schreibt Reward Event
9. Buddy zeigt Ergebnis emotional an
```

## Neue Architekturdateien

```txt
docs/architecture/AR_CONTEXTUAL_MISSION_REWARD_FLOW.md
docs/architecture/AR_LOCATION_RADIUS_AND_RALLY_GENERATION.md
docs/architecture/AR_QUESTION_MEMORY_MODEL.md
docs/architecture/AR_REWARD_POLICY_INPUT.md
docs/architecture/AR_QUESTION_EVIDENCE_EVENT.md
docs/architecture/AR_REWARD_PREVIEW_API.md
docs/architecture/AR_REWARD_LEDGER_EVENT.md
docs/architecture/AR_RIDDLE_FIRESTORE_SECURITY_PLAN.md
docs/architecture/AR_RIDDLE_FIRESTORE_RULES_DRAFT.md
docs/architecture/AR_RIDDLE_EMULATOR_TEST_PLAN.md
docs/architecture/USER_POINTS_CLIENT_WRITE_REFACTOR.md
docs/architecture/USER_ECONOMY_WRITE_SEARCH_NOTES.md
```

## TypeScript-Typenanker

```txt
lib/arRiddle/arRiddleBaseTypes.ts
lib/arRiddle/arQuestionEvidenceTypes.ts
lib/arRiddle/arQuestionMemoryTypes.ts
lib/arRiddle/arRewardPolicyTypes.ts
lib/arRiddle/arRallyGenerationTypes.ts
lib/arRiddle/index.ts
```

Status:

- [x] `ArQuestionEvidenceEvent` definiert.
- [x] `ArQuestionMemory` definiert.
- [x] `ArRewardPolicyInput` definiert.
- [x] `ArRewardPolicyResult` definiert.
- [x] `ArRallyGenerationInput` / `ArRallyGenerationDraft` definiert.
- [x] Barrel-Exports fuer `lib/arRiddle` angelegt.

## Harte Nicht-Erlaubt-Regeln

```txt
Unity setzt keine Punkte
Kamera setzt keine Punkte
Buddy-KI setzt keine Punkte
Frontend setzt keine Punkte
Client entscheidet keine Mission Completion
Client entscheidet keine Rewardhoehe
Client schreibt keine Reward-Ledger-Events
```

## Beispiel Blattfrage

Der Buddy darf sagen:

```txt
Hey, schau mal, was ist das fuer ein Blatt?
```

Die UI darf Antworten anzeigen:

```txt
Eichenblatt
Ahornblatt
Birkenblatt
```

Nach Antwort darf die App nur Evidence senden.

Nicht erlaubt:

```txt
Buddy/Kamera/UI sagt sofort: +3 Punkte garantiert
```

Erlaubt nach Serverantwort:

```txt
Backend hat +3 Punkte freigegeben
Buddy freut sich
UI zeigt +3 Punkte gutgeschrieben
```

## Reward Policy muss beruecksichtigen

```txt
Aufgabentyp
Schwierigkeit
Antwort korrekt/falsch
Nutzerhistorie
Buddy-/User-Level optional
Question Novelty
Location Novelty
Wiederholungsstrafe
Daily User Cap
Mission Type Cap
Systemreserve
Economy Health Score
Risk Level
Evidence Quality
Radius-/Sicherheitskontext
```

## Question Memory muss speichern

```txt
Welche Frage wurde gestellt?
Welches Objekt wurde erkannt?
An welchem Ort/Gebiet wurde gefragt?
Wie oft wurde sie gestellt?
Wie oft richtig/falsch beantwortet?
Wann darf sie wiederkommen?
Muss die Schwierigkeit steigen?
Gibt es Farming-Risiko?
```

## Firestore-/Security-Plan

Geplante Collections:

```txt
arQuestionEvidence
arQuestionMemory
arRewardPreviews
arRewardLedgerEvents
arRallyDrafts
```

Wichtig:

```txt
Client darf Evidence senden, aber nur mit false-Flags.
Client darf kein Memory schreiben.
Client darf keine Preview schreiben.
Client darf niemals Ledger schreiben.
```

## Separater Refactor-Risikoanker

Aktuell sind in bestehenden Rules historisch noch alte Client-Schreibrechte auf User-Economy-Felder sichtbar.

```txt
/users.points
/users.xp
/users.level
/users.energy
```

Dazu existieren jetzt:

```txt
docs/architecture/USER_POINTS_CLIENT_WRITE_REFACTOR.md
docs/architecture/USER_ECONOMY_WRITE_SEARCH_NOTES.md
```

Ziel: spaeter entfernen oder auf serverseitige Ledger-/UserStats-Ableitung umstellen.

Erster Suchstand:

```txt
Konkrete neue AR-Riddle-Client-Schreibstellen fuer Punkte/XP wurden nicht gefunden.
Sichtbarer Risikoanker bleibt firestore.rules mit historischen /users-Economy-Feldern.
```

## Naechste technische Micro-Tasks

- [x] TypeScript-Typen fuer `ArQuestionEvidenceEvent` definieren.
- [x] TypeScript-Typen fuer `ArQuestionMemory` definieren.
- [x] TypeScript-Typen fuer `ArRewardPolicyInput` und `ArRewardPolicyResult` definieren.
- [x] TypeScript-Typen fuer ortsnahe Rallye-Generierung definieren.
- [x] Serverseitigen Reward Preview Flow fuer AR-Fragen planen.
- [x] Server-Ledger-Event fuer AR-Fragen planen.
- [x] Firestore Rules fuer spaetere Evidence-/Ledger-Collections planen.
- [x] Konkrete Firestore Rule-Blöcke entwerfen.
- [x] Emulator-Testplan fuer AR-Riddle Collections ergaenzen.
- [x] Separaten Refactor-Risikoanker fuer alte `/users.points`-/`xp`-Client-Schreibrechte anlegen.
- [x] Erste Suche nach direkten User-Economy-Schreibstellen dokumentieren.
- [ ] UI bleibt bis dahin Preview-only.
- [ ] Bestehende direkte User-Economy-Schreibstellen tiefer in App-Routen/Hooks pruefen.

## Build-/Security-Hinweis

Alle aktuellen Mobile-/AR-/Buddy-Flows bleiben Preview/Evidence. Es darf keine produktive Punktevergabe im Client entstehen, bevor der serverseitige Reward-/Ledger-Pfad existiert.
