# J8.4C – AppShell und Produktmodul-Skalierung Addendum

Stand: 2026-05-05

## Vorabprüfung

[x] Vor Umsetzung geprüft: Es gab keinen bestehenden `AppShell`, `AppLayout`, `DashboardShell`, `LoggedInLayout` oder vergleichbaren gemeinsamen eingeloggten Layout-Baustein.
[x] Vorhanden waren bereits `AppSidebar.tsx` und `AppFooter.tsx`.
[x] Die neu angelegten Produktmodul-Seiten `/marktplatz`, `/leaderboard`, `/punkte-shop` und `/analytics` nutzten zunächst eine gemeinsame `ProductModulePlaceholderPage`.

## Umsetzung

[x] `app/components/AppShell.tsx` angelegt.
[x] `AppShell` kapselt gemeinsamen eingeloggten Seitenrahmen:
    - Helligkeitszustand
    - Hintergrundverlauf
    - Sidebar
    - Content-Bereich
    - Footer
[x] `ProductModulePlaceholderPage` nutzt jetzt `AppShell` statt Sidebar/Footer direkt zu duplizieren.
[x] Mehrere `page.tsx`-Routen bleiben bestehen, aber die gemeinsame Shell verhindert Layout-Dopplung.

## Skalierungsregel

[!] Neue eingeloggte Seiten sollen künftig zuerst prüfen, ob `AppShell` verwendbar ist.
[!] `page.tsx` bleibt pro Route erlaubt und gewünscht, soll aber nur Orchestrierung, Datenanschluss und Seitenkomposition übernehmen.
[!] Wiederkehrende Layouts, Karten, Hooks, Mapper und Types sollen nicht in `page.tsx` wachsen, sondern in `components/`, `hooks/`, `lib/` und `types.ts` ausgelagert werden.

## Offener Nachlauf

[ ] Build nach AppShell-Änderung lokal ausführen.
[ ] `/marktplatz`, `/leaderboard`, `/punkte-shop` und `/analytics` nach Build im Browser kurz prüfen.
[ ] Später prüfen, ob Dashboard, Einstellungen und Missionen schrittweise auf `AppShell` migriert werden können, ohne bestehende Logik zu beschädigen.

## KI-Fortsetzungs-Prompt

Lies zuerst `todolist/MASTER_PROMPT_FOR_AI.md`, `todolist/TODO_INDEX.md`, `todolist/NEXT_ACTIONS.md` und die fuehrenden Dateien: `todolist/MASTER_PROMPT_FOR_AI.md`, `todolist/NEXT_ACTIONS.md`, `todolist/TODO_INDEX.md`.

Arbeite mit dieser Datei nur ergaenzend und nachvollziehbar. Loesche keine alten Aufgaben, Roadmap-Punkte, Statushinweise oder erledigten Eintraege. Markiere veraltete oder doppelte Punkte nur als `veraltet`, `duplikat`, `erledigt`, `offen` oder `zu pruefen`.

Wenn du offene Punkte aus dieser Datei uebernimmst, verlinke sie in `todolist/TODO_INDEX.md` oder uebertrage sie nach `todolist/NEXT_ACTIONS.md`. Dokumentiere erledigte Arbeit in `todolist/DONE_LOG.md`.

