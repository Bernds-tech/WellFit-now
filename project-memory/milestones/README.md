# WellFit-now Milestone Snapshots

Append-only accepted milestone snapshots live here.

Create a snapshot when a major gate reaches `ACCEPTED` or `PRODUCTION_CONFIRMED`. Bind the relevant commit/PR/build/runtime/provider evidence and list unresolved carry-forward items. Never rewrite history to make a milestone appear cleaner than it was.
